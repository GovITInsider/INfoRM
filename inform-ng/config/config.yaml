general:
  log_level: INFO
  poll_interval_seconds: 30

discovery:
  enabled: true

logging:
  log_file: logs/inform.log

monitoring:
  countbeforealarm: 2
  poll_interval_seconds: 30

web:
  auto_refresh_seconds: 30
  noc_auto_refresh_seconds: 30

security:
  secret_key: "secret key change me"
  token_expires_minutes: 480     # 8 hours
